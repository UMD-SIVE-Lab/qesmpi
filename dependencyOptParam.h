class dependencyOptParam
{
    public:
        std::string variable_name;
        std::string operand1;
        std::string operand2;
        char op;
        int idx;       /// single || range || set
};